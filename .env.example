# Isolated local PostgreSQL database. Never use a GenPlus production URL here.
DATABASE_URL="postgresql://megtras_demo:change_this_local_password@127.0.0.1:55432/megtras_job_management_demo?schema=public"

# Local application URLs and authentication.
NEXTAUTH_URL="http://localhost:3100"
NEXTAUTH_SECRET="replace-with-a-random-demo-only-secret"
NEXT_PUBLIC_APP_URL="http://localhost:3100"

# Local-only file storage.
UPLOAD_DIR="./demo-data/uploads"

# Offline/task settings.
GEOFENCE_RADIUS_METERS="200"
MIN_EVIDENCE_PHOTOS="3"
NEXT_PUBLIC_MAPS_PROVIDER="leaflet"
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=""
NEXT_PUBLIC_GOOGLE_MAPS_MAP_ID=""

# External communications are opt-in and disabled for this demo.
ENABLE_EXTERNAL_EMAIL="false"
ENABLE_EXTERNAL_WHATSAPP="false"
NEXT_PUBLIC_ENABLE_EXTERNAL_WHATSAPP="false"
SMTP_HOST=""
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM=""
